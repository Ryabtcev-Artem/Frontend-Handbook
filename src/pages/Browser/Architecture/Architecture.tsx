import canvas from '../../../images/BrowserImages/Architecture/canvas.png'

import NavButtons from "../../../components/NavButtons/NavButtons.tsx";
type HowBrowserWorkProps = {}
export default function Architecture(props: HowBrowserWorkProps) {
  const {} = props
  return (
    <section className="topic container">
      <h2>Архитектура браузера</h2>

      <p>Современный браузер — это не просто окно для просмотра сайтов, а целая экосистема из модулей, которые работают вместе. У него сложная внутренняя структура, где каждый компонент выполняет свою функцию: от обработки адреса до запуска скриптов и вывода картинки на экран.</p>

      <h3>Основные компоненты браузера</h3>
      <ul>
        <li>
          <strong>Интерфейс пользователя (UI)</strong> — всё, что видит и с чем взаимодействует пользователь: адресная строка, вкладки, кнопки назад/вперёд, меню, настройки и т. д.
        </li>

        <li>
          <strong>Браузерный движок</strong> — координирует взаимодействие между UI, рендерингом, сетевыми запросами и JavaScript. Это центр всей архитектуры.
        </li>

        <li>
          <strong>Рендеринг-движок (Rendering Engine)</strong> — Ключевая часть любого веб-браузера, которая отвечает за отображение веб-страниц. Преобразует HTML и CSS в визуальный интерфейс, а также взаимодействует с JavaScript для динамического изменения содержимого страницы. Примеры: Blink, WebKit, Gecko.
        </li>
        <li>
          <strong>Сетевой модуль</strong> — обрабатывает сетевые запросы (например, загрузку HTML, CSS, JS, изображений и видео) через протоколы вроде HTTP и HTTPS. Отвечает за историю сайтов, за взаимодействие с DNS серверов и т.д.
        </li>
        <li>
          <strong>JavaScript-движок</strong> — Обрабатывает JavaScript код, запускает скрипты на странице. Примеры: V8 (Chrome), SpiderMonkey (Firefox), JavaScriptCore (Safari).
        </li>

        <li>
          <strong>UI Backend</strong> — это часть браузера, которая отвечает за внутреннюю логику самого браузера: управление окнами, вкладками, настройками, историей, интерфейсом и взаимодействием между остальными модулями. Это своего рода "сердце" браузера, откуда всё координируется.
        </li>

        <li>
          <strong>Система хранения данных</strong> — включает куки, localStorage, sessionStorage, IndexedDB и кэш. Обеспечивает сохранение данных между сессиями.
        </li>
      </ul>
      <img
        src={canvas}
        alt=""
        width=""
        height=""
        loading="lazy"
      />
      <h3>Как это всё работает вместе?</h3>
      <p>Когда вы вводите адрес сайта:</p>
      <ol>
        <li>Интерфейс передаёт адрес в движок браузера.</li>
        <li>Движок запускает сетевой модуль, чтобы получить HTML и другие файлы.</li>
        <li>HTML передаётся в рендеринг-движок, который строит визуальную страницу.</li>
        <li>JavaScript запускается в отдельном движке и может влиять на DOM и внешний вид страницы.</li>
        <li>Всё это работает в изолированном процессе, чтобы сохранить стабильность и безопасность.</li>
      </ol>
      <h2>Итого</h2>
      <ul>
        <li>Браузер состоит из множества модулей: интерфейс, движок, рендеринг, сеть, скрипты, хранилище и др.</li>
        <li>Каждый модуль выполняет свою задачу, и все вместе создают полноценный инструмент для работы с вебом.</li>
        <li>Браузер — это не просто окно, а мощная система с продуманной архитектурой и высокой сложностью.</li>
      </ul>
      <NavButtons prev={'/browser/Intro'} next={'/browser/Cookies'} />
    </section>
  )
}