import NavButtons from "../../../components/NavButtons/NavButtons.tsx";

type IpAdressProps = {}
export default function IpAddress(props: IpAdressProps) {
  const {} = props

  return (
    <section className="topic container">
      <h2 className="topic__title">Что такое IP-адрес</h2>
      <p>
        <strong>IP-адрес</strong> (от англ. <em>Internet Protocol address</em>) — это уникальный числовой адрес устройства в компьютерной сети. Он используется, чтобы передавать данные между устройствами: как почтовый адрес для писем, IP указывает, куда отправить цифровую «посылку».
      </p>

      <p>Когда вы открываете сайт или отправляете сообщение, ваш компьютер «подписывает» этот запрос своим IP-адресом, а в качестве получателя указывает IP нужного сервера. Без IP-адресов данные не знали бы, куда идти, и интернет просто не работал бы.</p>

      <p>
        <strong>Простой пример:</strong> представьте, что вы отправляете письмо другу. Вы пишете его адрес на конверте, и почта доставляет письмо точно по этому адресу. В интернете IP-адрес — это и есть тот самый «конвертный» адрес.
      </p>

      <h3>Формат IP-адреса: IPv4 и IPv6</h3>
      <p>
        <strong>IPv4</strong> — старый и до сих пор самый распространённый формат. Он состоит из четырёх чисел от 0 до 255, разделённых точками. Например: <code>192.168.0.1</code>.
      </p>
      <p>
        Всего таких адресов около 4,3 миллиарда, и их стало не хватать.
      </p>
      <p>
        <strong>IPv6</strong> — новый формат, состоящий из восьми групп по четыре символа в шестнадцатеричном виде, разделённых двоеточиями. Например: <code>2001:0db8:85a3:0000:0000:8a2e:0370:7334</code>. Он позволяет использовать около 340 ундециллионов (10^36) уникальных адресов.
      </p>

      <h3>Статический и динамический IP</h3>
      <p>
        <strong>Статический IP</strong> — постоянный, закреплён за устройством и не меняется. Такой адрес часто используют серверы и важные сервисы.
      </p>
      <p>
        <strong>Динамический IP</strong> — временный, выдается провайдером при подключении. Это удобно и экономно, потому что большинству пользователей не нужен постоянный адрес.
      </p>
      <h3>А что такое порт?</h3>
      <p>
        <strong>Порт</strong> — это «внутренний номер» на устройстве, к которому приходит соединение. Он дописывается в конце IP <code>192.168.0.1:<strong style={{textDecoration: 'underline'}}>443</strong></code> Если IP — это адрес дома, то порт — это номер квартиры. Один компьютер (один IP) может обслуживать много соединений одновременно, и каждый из них идёт через свой порт.
      </p>
      <p>
        Например, веб-сайты по умолчанию работают через <code>порт 80</code> (HTTP) или <code>порт 443</code> (HTTPS). А если вы запускаете видеозвонок, он идёт через другой порт.
      </p>

      <p><strong>Простой пример:</strong> У одного дома (IP) много квартир (порты). Почтальон приходит по адресу, а консьерж говорит: «Вам в 443-ю» — и письмо точно доходит до нужного получателя.</p>

      <h3>Итого</h3>
      <ul>
        <li>IP-адрес — это уникальный адрес устройства в сети.</li>
        <li>IPv4: формат <code>192.168.0.1</code>, 4,3 млрд адресов.</li>
        <li>IPv6: формат <code>2001:db8::7334</code>, невероятно много адресов.
        </li>
        <li>IP-адрес может быть постоянным (статическим) или временным (динамическим).</li>
        <li>Порт — это конкретная «дверь» внутри устройства, куда приходят разные типы соединений (веб, видео, почта и т.д.).</li>
      </ul>
      <NavButtons prev={'/internet/Connection'} next={'/internet/Dns'} />
    </section>
  )
}