import NavButtons from "../../../components/NavButtons/NavButtons.tsx";
import websocketvshttp from '../../../images/InternetImages/WebSocket/websocketvshttp.png'
type WebSocketProps = {}
export default function WebSocket(props: WebSocketProps) {
  const {} = props

  return (
    <section className={'topic container'}>
      <h2>WebSocket</h2>
      <p>
        <strong>WebSocket</strong> — это сетевой протокол, который устанавливает постоянное двунаправленное (full-duplex) соединение между клиентом и сервером поверх TCP. После установки соединения обе стороны могут отправлять и получать данные в любое время без необходимости повторного подключения.
      </p>
      <p>
        Иными словами, WebSocket создаёт «живой» канал связи, по которому клиент и сервер общаются напрямую и непрерывно.
      </p>
      <h3>Как работает WebSocket?</h3>
      <h4>📶 Пошаговый процесс работы WebSocket:</h4>
      <ol>
        <li>
          <strong>Инициация соединения (рукопожатие)</strong><br />
          Всё начинается с обычного HTTP-запроса с заголовком <code>Upgrade: websocket</code>. Клиент говорит серверу: «Я хочу перейти на протокол WebSocket».
          <br />
          Пример заголовков:
          <pre>
GET /chat HTTP/1.1<br />
Host: server.example.com<br />
Upgrade: websocket<br />
Connection: Upgrade<br />
Sec-WebSocket-Key: x3JJHMbDL1EzLkh9GBhXDw==<br />
Sec-WebSocket-Version: 13
    </pre>
          <p>
            Если сервер поддерживает WebSocket, он отвечает с подтверждением перехода:
          </p>
          <pre>
HTTP/1.1 101 Switching Protocols<br />
Upgrade: websocket<br />
Connection: Upgrade<br />
Sec-WebSocket-Accept: HSmrc0sMlYUkAGmm5OPpG2HaGWk=
    </pre>
          <p>
            Это и есть <strong>WebSocket Handshake</strong> — переход с HTTP на WebSocket.
          </p>
        </li>
        <li>
          <strong>Установление постоянного соединения</strong><br />
          После успешного рукопожатия HTTP-соединение «апгрейдится» до WebSocket и остаётся открытым. Открытие подтверждается событием <code>onopen</code> на клиенте.
          <br />
          Теперь клиент и сервер могут свободно обмениваться данными <strong>без дополнительного запроса</strong> каждый раз.
        </li>
        <li>
          <strong>Формат сообщений</strong><br />
          WebSocket передаёт данные в виде <strong>фреймов (frames)</strong>. Это могут быть текстовые или бинарные данные.
          <ul>
            <li><code>Text Frame</code> — JSON, строки и т.п.</li>
            <li><code>Binary Frame</code> — изображения, буферы, файлы и т.д.</li>
            <li><code>Ping/Pong Frame</code> — для проверки, что соединение живо</li>
            <li><code>Close Frame</code> — когда соединение закрывается</li>
          </ul>
          Это делает WebSocket удобным для real-time-приложений: чат, биржа, игры, обновления без перезагрузки.
        </li>

        <li>
          <strong>Двусторонняя коммуникация</strong><br />
          Обе стороны — клиент и сервер — могут отправлять сообщения в любое время, независимо друг от друга.
          <br />
          Пример на клиенте:
          <pre>
socket.send({`${JSON.stringify({ type: "ping" })}`}))
    </pre>
          Пример на сервере:
          <pre>
socket.send("Welcome, user!")
    </pre>
        </li>

        <li>
          <strong>Закрытие соединения</strong><br />
          В любой момент соединение можно закрыть с одной из сторон. Это делает WebSocket экономичным: оно живёт, пока нужно, а не бесконечно.
          <br />
          На клиенте:
          <pre>
socket.close()
    </pre>
          Можно передать <code>reason</code> и <code>code</code>, чтобы объяснить причину закрытия.
          <br />
          Событие <code>onclose</code> поможет отреагировать на закрытие и повторно подключиться при необходимости.
        </li>
      </ol>
      <figure>
        <img
          src={websocketvshttp}
          alt=""
          width=""
          height=""
          loading="lazy"
        />
        <figcaption><em>Разница между HTTP и WebSocket соединениями</em></figcaption>
      </figure>
      <p>
        📌 В отличие от REST, который основан на HTTP, и где каждое взаимодействие требует нового запроса, WebSocket работает как <strong>живой канал связи</strong>. Это делает его особенно полезным там, где важна скорость реакции и частые обновления.
      </p>

      <p>
        <em>Пример:</em> WebSocket как телефонный звонок, во время которого оба участника могут говорить и слушать одновременно. А HTTP — это обычное письмо, которое нужно написать, отправить и ждать ответ.
      </p>
      <h3>Что такое Short Polling?</h3>
      <p>
        <strong>Short polling</strong> — это метод периодического опроса сервера клиентом, при котором клиент отправляет запросы с фиксированным интервалом, чтобы проверить наличие новых данных.
      </p>
      <p>
        <em>Пояснение:</em> Клиент спрашивает сервер: «Есть ли у тебя новые данные?» и получает ответ «да» или «нет». Если нет, через определённое время клиент задаёт вопрос снова.
      </p>
      <p>
        <em>Пример:</em> Представьте, что вы ждёте письмо от друга, и раз в десять минут ходите на почту, чтобы проверить, есть ли новое письмо.
      </p>

      <h3>Что такое Long Polling?</h3>
      <p>
        <strong>Long polling</strong> — это метод, при котором клиент отправляет запрос на сервер и ждёт ответа, пока сервер не будет готов отправить новые данные или не истечёт таймаут соединения.
      </p>
      <p>
        Как только сервер получает новые данные, он немедленно отправляет ответ клиенту. Клиент после получения ответа сразу же открывает новое соединение и повторяет процесс.
      </p>
      <p>
        <em>Пример:</em> Вы позвонили другу и договорились, что он не положит трубку, пока у него не появится важная новость. Как только новость появляется — он сразу сообщает вам и кладёт трубку, после чего вы звоните снова.
      </p>

      <h3>Сравнение Short Polling, Long Polling и WebSocket</h3>
      <p>
        Все три метода служат для обмена данными между клиентом и сервером, но работают по-разному:
      </p>
      <ul>
        <li>
          <strong>Short Polling</strong> — простой, но неэффективный. Клиент часто отправляет пустые запросы без новых данных, что создаёт лишнюю нагрузку.
        </li>
        <li>
          <strong>Long Polling</strong> — более эффективный, так как сервер держит соединение открытым и отвечает только при появлении данных, но при каждом ответе создаётся новое соединение.
        </li>
        <li>
          <strong>WebSocket</strong> — самый эффективный, поскольку устанавливает постоянное соединение, по которому данные могут поступать в любой момент без задержек на повторное подключение.
        </li>
      </ul>

      <h2>Итого</h2>
      <ul>
        <li>WebSocket работает так:</li>
        <ol>
          <ol>
            <li>Клиент посылает специальный HTTP-запрос с заголовком <code>Upgrade: websocket</code>, чтобы перейти на протокол WebSocket.
            </li>
            <li>Сервер принимает этот запрос и устанавливает постоянное соединение по протоколу WebSocket.</li>
            <li>После установления соединения клиент и сервер могут обмениваться сообщениями напрямую и асинхронно, без повторных подключений.</li>
            <li>Соединение остаётся открытым до тех пор, пока одна из сторон его не закроет.</li>
          </ol>
        </ol>
        <li>Short polling и long polling — это способы, используемые там, где WebSocket недоступен или не нужен, но они менее эффективны.</li>
      </ul>
      <NavButtons
        prev={'/internet/RestApi'}
        next={'/internet/GraphQl'}
      />
    </section>
  )
}
