import VsCode from "../../../components/VsCode/VsCode.tsx";
import NavButtons from "../../../components/NavButtons/NavButtons.tsx";
export default function Config() {

  return (
    <section className={'topic container'}>
      <h2>Конфигурация и инициализация Git</h2>

      <h3>Что такое конфигурация Git</h3>
      <p>
        Git — это инструмент, который нужно немного настроить под себя. Он должен знать, кто вы, чтобы подписывать ваши коммиты. Эта настройка
        называется конфигурацией. Один раз указали имя и почту — и каждое ваше изменение будет зафиксировано с авторством.
      </p>

      <p>
        Пример конфигурации:
      </p>

      <VsCode>{`git config --global user.name "Ваше Имя"`}</VsCode>
      <VsCode>{`git config --global user.email "ваш@email.com"`}</VsCode>

      <p>
        Флаг <code>--global</code> означает, что настройки применяются ко всем проектам на этом компьютере. Если нужно настроить только один проект —
        просто не указывайте этот флаг.
      </p>

      <h3>Что делает git init</h3>
      <p>
        Команда <code>git init</code> запускает Git внутри вашей папки. Она создаёт скрытую папку <code>.git</code> — это и есть сердце Git-проекта.
        Там хранится вся история, все ветки, все коммиты и настройки проекта.
      </p>

      <p>
        После <code>git init</code> ваша папка становится «репозиторием». Теперь Git будет следить за файлами и запоминать изменения.
      </p>

      <p>
        Пример:
      </p>

      <VsCode>{`mkdir my-project`}</VsCode>
      <VsCode>{`cd my-project`}</VsCode>
      <VsCode>{`git init`}</VsCode>

      <p>
        После этого можно добавлять файлы, делать коммиты и полноценно пользоваться всеми возможностями Git.
      </p>

      <h3>Локальный vs глобальный уровень</h3>
      <p>
        Git имеет несколько уровней конфигурации:
      </p>
      <ul>
        <li><strong>--global</strong> — настройки для всех проектов от вашего имени.</li>
        <li><strong>--local</strong> — настройки только для одного конкретного проекта.</li>
      </ul>
      <p>
        Git всегда сначала смотрит на локальные настройки. Если их нет — берёт глобальные. Это удобно: можно указать одни данные для всех репозиториев,
        а потом при необходимости переопределить их в конкретном.
      </p>

      <h3>Проверка текущей конфигурации</h3>
      <p>
        Чтобы посмотреть, как всё сейчас настроено, используйте:
      </p>

      <VsCode>{`git config --list`}</VsCode>

      <p>
        Вы увидите список всех текущих параметров: имя, почта, редактор и т.д. Если что-то нужно поменять — просто задайте новое значение через
        <code>git config</code>.
      </p>

      <h2>Итого</h2>
      <ul>
        <li>Перед работой с Git нужно указать имя и почту — это конфигурация.</li>
        <li><code>git init</code> превращает обычную папку в Git-проект, создавая системную папку <code>.git</code>.</li>
        <li>Конфигурация бывает глобальной (для всех проектов) и локальной (для одного).</li>
        <li>Настройки легко проверяются командой <code>git config --list</code>.</li>
        <li>Без инициализации Git не будет отслеживать изменения — сначала <code>git init</code>, потом работа.</li>
      </ul>
      <NavButtons prev={'/git/Intro'} next={'/git/Save'}/>
    </section>
  )
}
