import NavButtons from "../../../components/NavButtons/NavButtons.tsx";

type AccessibilityProps = {}
export default function Accessibility(props: AccessibilityProps) {
  const {} = props

  return (
    <section className={'topic container'}>
      <h2>Доступность (Accessibility) в HTML</h2>

      <p>
        Доступность (или <strong>Accessibility</strong>, часто сокращают до <strong>a11y</strong>) — это практика создания сайтов и интерфейсов, которые могут удобно использовать все люди, включая пользователей с инвалидностью. Сюда входят люди с нарушениями зрения, слуха, моторики, когнитивных функций и других особенностей.
      </p>

      <p>
        Говоря проще: доступный сайт — это тот, которым сможет пользоваться <em>любой</em>, без барьеров. Даже если человек управляет компьютером с помощью клавиатуры, экранного диктора или вообще голосом.
      </p>

      <h3>Зачем это нужно?</h3>
      <ul>
        <li><strong>Это честно.</strong> Интернет — для всех. Ограничения в интерфейсе не должны отсеивать часть людей.</li>
        <li><strong>Это законно.</strong> Во многих странах (например, в США и ЕС) доступность — это юридическое требование для гос. сайтов и коммерческих сервисов.</li>
        <li><strong>Это полезно всем.</strong> Удобная навигация с клавиатуры, чёткая структура и понятные элементы делают сайт комфортнее даже для обычного пользователя.</li>
      </ul>

      <h3>Как HTML помогает сделать сайт доступным?</h3>
      <p>
        HTML предлагает встроенные средства для улучшения доступности, а когда их недостаточно — используются специальные атрибуты <strong>ARIA</strong> (Accessible Rich Internet Applications).
        Ниже — самые важные и полезные.
      </p>

      <h4>1. <code>alt</code> — текстовое описание изображения</h4>
      <p>
        Атрибут <code>alt</code> используется в теге <code>&lt;img&gt;</code> и описывает, что изображено на картинке.
        Это важно для тех, кто не видит изображение (например, незрячие пользователи или при медленном интернете).
      </p>
      <pre><code>&lt;img src="cat.jpg" alt="Серый кот спит на подоконнике"&gt;</code></pre>
      <p>
        Если изображение несёт чисто декоративную функцию — <code>alt=""</code>. Это говорит экранным дикторам: можно пропустить.
      </p>

      <h4>2. <code>aria-label</code> — подпись для элемента</h4>
      <p>
        Добавляет описание для элемента, даже если оно не видно на экране. Часто используется на иконках-кнопках или ссылках без текста.
      </p>
      <pre><code>&lt;button aria-label="Открыть меню"&gt;
        ☰
        &lt;/button&gt;</code></pre>

      <h4>3. <code>aria-hidden</code> — скрытие элемента от скринридеров</h4>
      <p>
        Если какой-то элемент есть в DOM, но его <strong>не нужно озвучивать</strong>, используйте <code>aria-hidden="true"</code>.
        Это может быть, например, декоративный текст или дублирующий контент.
      </p>
      <pre><code>&lt;div aria-hidden="true"&gt;★&lt;/div&gt;</code></pre>

      <h4>4. <code>role</code> — указание роли элемента</h4>
      <p>
        В некоторых случаях (например, при кастомных компонентах) HTML-тег сам по себе не несёт смысла. Атрибут <code>role</code> сообщает, как следует воспринимать этот элемент.
      </p>
      <pre><code>&lt;div role="button"&gt;Нажми меня&lt;/div&gt;</code></pre>
      <p>
        Но важно: если есть подходящий HTML-тег (<code>&lt;button&gt;</code>), <strong>используйте его</strong>, а не роль — так надёжнее.
      </p>

      <h4>5. <code>tabindex</code> — управление порядком фокуса</h4>
      <p>
        Этот атрибут позволяет управлять тем, как пользователь переходит между элементами с помощью клавиши <kbd>Tab</kbd>.
      </p>
      <ul>
        <li><code>tabindex="0"</code> — элемент можно сфокусировать, он вписывается в естественный порядок.</li>
        <li><code>tabindex="-1"</code> — элемент нельзя сфокусировать с клавиатуры (но можно через JavaScript).</li>
        <li><code>tabindex="1"</code> и выше — задаёт точный порядок, но использовать <strong>не рекомендуется</strong> без крайней необходимости.</li>
      </ul>

      <h4>6. <code>aria-labelledby</code> и <code>aria-describedby</code></h4>
      <p>
        Эти два атрибута позволяют связать элемент с другим — например, с заголовком или подсказкой.
      </p>
      <pre><code>&lt;div id="title"&gt;Подписка на рассылку&lt;/div&gt;
        &lt;input aria-labelledby="title"&gt;</code></pre>
      <pre><code>&lt;small id="hint"&gt;Ваш e-mail не будет опубликован&lt;/small&gt;
        &lt;input aria-describedby="hint"&gt;</code></pre>

      <h3>📝 Общие рекомендации</h3>
      <ul>
        <li>По возможности, используйте <strong>стандартные HTML-теги</strong> (например, <code>&lt;button&gt;</code>, <code>&lt;form&gt;</code>, <code>&lt;label&gt;</code>) — они уже поддерживают доступность.</li>
        <li>Не скрывайте важный контент с помощью <code>display: none</code> или <code>visibility: hidden</code>, если он должен быть озвучен.</li>
        <li>Проверяйте сайт с клавиатуры и экранным диктором (например, NVDA, VoiceOver, ChromeVox).</li>
        <li>Добавляйте <strong>подписи</strong> ко всем полям ввода.</li>
      </ul>

      <ul>
        <li>
          <code>&lt;button&gt;</code>, <code>&lt;a&gt;</code>, <code>&lt;label&gt;</code> — это не просто визуальные элементы, они встроенно поддерживают навигацию и взаимодействие с помощью клавиатуры и экранных читалок.
        </li>
        <li>
          Семантические теги (<code>&lt;header&gt;</code>, <code>&lt;nav&gt;</code>, <code>&lt;main&gt;</code>, <code>&lt;footer&gt;</code>) помогают описать структуру страницы, и скринридеры используют эту информацию, чтобы озвучивать её правильно.
        </li>
        <li>
          Атрибуты <code>alt</code> у изображений, <code>aria-label</code>, <code>aria-hidden</code>, <code>role</code> и другие ARIA-атрибуты позволяют точно описать, что делает элемент, даже если это не видно визуально.
        </li>
      </ul>

      <h3>🎧 Пример: как это воспринимает экранный диктор</h3>
      <p>
        Представьте, что вы не видите экран. Вы перемещаетесь по странице с помощью клавиш Tab и стрелок, а голосовой помощник зачитывает:
      </p>
      <blockquote>
        "Заголовок первого уровня: Добро пожаловать. Кнопка: Оформить заказ. Ссылка: Подробнее о товаре."
      </blockquote>
      <p>
        Если кнопка сделана «на диве» и не объявлена как кнопка, пользователь вообще может не узнать, что на неё можно нажать.
      </p>

      <h3>🧑‍🦽 Доступность — это не отдельная надстройка</h3>
      <p>
        Это не «функция для особенных» — это часть качественного веб-разработки. Как адаптивность под мобильные устройства или безопасность. Если вы делаете интерфейс для людей — он должен работать для всех.
      </p>

      <h2>Итого</h2>
      <ul>
        <li><strong>Accessibility (a11y)</strong> — это доступность сайтов для людей с разными возможностями.</li>
        <li>Правильная HTML-разметка уже содержит всё необходимое для базовой доступности.</li>
        <li>Семантика, кнопки, ссылки, подписи, альтернативные тексты — всё это важно.</li>
        <li>Доступный сайт — это не только забота, но и качество, которое почувствуют все пользователи.</li>
      </ul>
      <NavButtons prev={'/html/Semantic'} next={'/html/Bem'} />
    </section>
  )
}