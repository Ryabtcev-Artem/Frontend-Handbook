import dom from '../../../images/HtmlImages/Intro/dom.jpg'
import NavButtons from "../../../components/NavButtons/NavButtons.tsx";

export default function Intro() {
  

  return (
    <section className="topic container">
      <h2>Что такое HTML?</h2>
      <p>
        HTML (HyperText Markup Language) — это стандартизированный язык разметки документов для интернета. Он описывает структуру веб-страницы с помощью "тегов" — специальных обозначений, которые говорят браузеру, как отображать содержимое: заголовки, абзацы, изображения, списки, таблицы и так далее.
      </p>

      <h3>Почему именно HyperText Markup Language?</h3>
      <p>
        Название HTML — это такой привет из прошлого. Когда язык только появился, главная фишка была в <strong>гипертексте</strong>: можно было вставить ссылку в один текст и перейти по ней на другой. В те времена это была настоящая революция — текст, который ведёт куда-то дальше. Не книга, где всё строго по страницам, а паутина, где ты прыгаешь от идеи к идее.
      </p>
      <p>
        Слово <strong>Markup</strong> (разметка) появилось потому, что HTML не "рисует", не "программирует", а просто <em>отмечает</em>, где заголовок, где список, где картинка.
      </p>
      <p>
        Но со временем HTML вырос. Теперь он описывает не только гипертекст — он описывает вообще всё, что мы видим на веб-странице. Кнопки, формы, таблицы, видео, структуры интерфейса. Название осталось, потому что его уже везде знали. Хотя, по сути, это давно уже не просто язык гипертекста — это <strong>язык веба</strong>.
      </p>

      <h3>HTML и DOM</h3>
      <p>
        Когда браузер получает HTML-файл, он превращает его в структуру данных, называемую <strong>DOM (Document Object Model)</strong>. Это древовидная модель, в которой каждый HTML-элемент становится узлом дерева. Эта структура позволяет JavaScript взаимодействовать с HTML: изменять содержимое, стили, добавлять или удалять элементы.
      </p>
      <p>Вот, пример кода на HTML, и его интерпретация в виде DOM:</p>
      <pre>
        {`<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>Пример DOM-структуры</title>
  </head>
  <body>
    <h1>Главный заголовок</h1>
    <h2>Подзаголовок</h2>
  </body>
</html>`}
      </pre>
      <figure>
        <img
          className={'invert'}
          src={dom}
          alt=""
          width=""
          height=""
          loading="lazy"
        />
        <figcaption><em>Схема DOM дерева, соответсвующая HTML-коду выше</em></figcaption>
      </figure>
      <p>На большинстве базовых схем DOM-дерева не показывают текстовые узлы, чтобы не загромождать визуал.

        Но на самом деле у каждого элемента, в котором есть текст (например, <code>{`<h1>Главный заголовок</h1>`}</code>), в DOM-структуре есть дочерний текстовый узел. Он не нарисован на схеме для простоты.</p>

      <h2>Итого</h2>
      <ul>
        <li>
          <strong>HTML</strong> — язык разметки, описывающий структуру веб-страницы.
        </li>
        <li>HTML не программирует поведение, а создаёт каркас контента.</li>
        <li>После загрузки HTML преобразуется в <strong>DOM</strong> — структуру, с которой работает браузер и JavaScript.
        </li>
        <li>Каждый HTML-документ состоит из тегов, которые указывают, что и как должно быть отображено на странице.</li>
      </ul>
      <NavButtons prev={'/html'} next={'/html/Structure'} />
    </section>
  )
}