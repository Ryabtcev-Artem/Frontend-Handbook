import VsCode from "../../../components/VsCode/VsCode.tsx";
import NavButtons from "../../../components/NavButtons/NavButtons.tsx";
import adapters from "../../../images/ReactImages/intro/adapters.png"
import multi from "../../../images/ReactImages/intro/multi.png"
import single from "../../../images/ReactImages/intro/single.png"

export default function Intro() {
  return (
    <section className={"topic container"}>
      <h2>Что такое React</h2>
      <p>
        React — это JavaScript-библиотека для создания пользовательских
        интерфейсов. Она позволяет строить интерфейсы из независимых
        компонентов, которые управляют своим состоянием и могут
        переиспользоваться в разных частях приложения. Следует понимать,
        что React<strong> независим</strong> от браузера. Он
        представляет собой библиотеку, которая отвечает за описание и
        управление состоянием интерфейса, но не диктует, как и где
        именно этот интерфейс будет отображён. Это означает, что React
        можно использовать не только для создания веб-приложений, но и в
        других средах:
      </p>
      <ul>
        <li>
          📱 <strong>React Native</strong> — позволяет использовать
          React для разработки мобильных приложений (iOS, Android), но
          рендерит не в HTML, а в нативные компоненты платформы.
        </li>
        <li>
          🖨️ <strong>React PDF</strong> — используется для создания
          PDF документов.
        </li>
        <li>
          💻 <strong>Ink</strong> — позволяет создавать интерфейсы в
          терминале с использованием компонентов React.
        </li>
      </ul>
      <img
        className={'invert'}
        src={adapters}
        alt=""
        width=""
        height=""
        loading="lazy"
      />
      <p>
        Важно понимать, что за визуализацию и рендеринг React
        компонентов в браузере отвечает библиотека <strong>React DOM</strong>, а не сам React.
      </p>
      <p>Проще говоря React — это <strong>библиотека описания UI</strong>, а то, куда и как он рендерится, уже отвечают другие библиотеки.
      </p>
      <h3>Multi и Single Page Application</h3>
      <p>Для того, чтобы понять, для чего вообще нужен React, стоит сначала разобраться с классическим подходом к построению веб-приложений.</p>
      <p>
        При разработке сложных интерфейсов на чистом JavaScript
        мы создаём несколько HTML-файлов. Каждый обладает каким-то своим содержимым, скриптами, стилями и загружается с сервера при переходе по ссылкам.
      </p>
      <p>
        Такой подход называется <strong>Multi Page Application (MPA)</strong> — приложение, где каждый переход по ссылке ведёт к полной перезагрузке страницы и загрузке нового HTML-документа с сервера.
      </p>
      <img
        className={'invert'}
        src={multi}
        alt=""
        width=""
        height=""
        loading="lazy"
      />


      <p>
        Основные проблемы MPA — это <em>медленная навигация</em>, загрузка повторяющихся элементов на странице, лишние запросы к серверу, потеря состояния и более сложная работа с клиентской логикой.
      </p>

      <p>
        Здесь на помощь приходит <strong>Single Page Application (SPA)</strong> — приложение, которое загружает единственный HTML-файл, а весь интерфейс обновляется динамически через JavaScript, без перезагрузки страницы.
      </p>
      <img
        className={"invert"}
        src={single}
        alt=""
        width=""
        height=""
        loading="lazy"
      />
      <p>
        Именно для построения таких SPA и используется React — он позволяет создавать динамические интерфейсы прямо внутри одной HTML-страницы, обновляя только необходимые части без полной перезагрузки, что делает приложение быстрым и отзывчивым.
      </p>
      <p>
        Однако у такого подхода также есть свои минусы: первичный размер загружаемого файла при открытии SPA может быть значительно больше, чем у MPA-приложения. Это связано с тем, что весь JavaScript-код, отвечающий за интерфейс и маршрутизацию, загружается сразу.
      </p>

      <p>
        Тем не менее, эту <strong>первичную загрузку можно оптимизировать</strong> с помощью таких техник, как <em>ленивая подгрузка компонентов (code splitting)</em>, <em>оптимизация бандла</em>, <em>кэширование</em> и <em>минимизация ресурсов</em>.
      </p>

      <p>
        Что касается SEO, то в классических SPA это может быть проблемой, так как поисковые боты не всегда хорошо обрабатывают динамически подгружаемый контент. Чтобы решить эту проблему, разработчики используют <strong>SSR (Server-Side Rendering)</strong> или <strong>SSG (Static Site Generation)</strong> — например, с помощью фреймворков вроде <code>Next.js</code>. Эти подходы позволяют отдавать предварительно отрендеренный HTML, что делает страницы доступными для индексации поисковыми системами.
      </p>
      <h3>Почему ванильный JS — не всегда хороший выбор</h3>
      <p>
        Ванильный JavaScript — это чистый JavaScript без использования сторонних библиотек и фреймворков. Он отлично подходит для простых скриптов и небольших страниц.
      </p>
      <p>
        Однако по мере роста приложения начинает проявляться множество проблем: запутанная структура кода, трудности с управлением состоянием, сложность в отслеживании изменений и множественные зависимости между элементами интерфейса.
      </p>
      <p>
        Давайте посмотрим, как это выглядит на простом примере работы с DOM.
      </p>
      <VsCode>{`// Ванильный JS - много ручной работы
const counter = document.getElementById('counter');
const button = document.getElementById('increment');
let count = 0;

button.addEventListener('click', () => {
  count++;
  counter.textContent = count;
  // И так для каждого элемента...
});`}</VsCode>

      <p>
        В большом приложении таких связей между данными и интерфейсом
        сотни. Отследить их все становится невозможно, а любое изменение
        может сломать что-то в другом месте.
      </p>

      <h3>Декларативный подход</h3>
      <p>
        React предлагает декларативный подход — вы описываете, как
        должен выглядеть интерфейс для каждого состояния данных, а
        библиотека сама разбирается, что нужно обновить в DOM.
      </p>

      <VsCode>{`// React - описываем желаемый результат
function Counter() {
  const [count, setCount] = useState(0);
  
  return (
    <div>
      <p>Счетчик: {count}</p>
      <button onClick={() => setCount(count + 1)}>
        Увеличить
      </button>
    </div>
  );
}`}</VsCode>

      <p>
        Вместо императивных команд "найди элемент, измени его
        содержимое" вы говорите "при таком состоянии интерфейс должен
        выглядеть так". React сам выяснит, что именно нужно изменить.
      </p>

      <h3>Компонентная архитектура</h3>
      <p>
        React строится на концепции компонентов — независимых фрагментов
        интерфейса со своей логикой и состоянием. Компонент возвращает разметку, описывающую, как он
        должен отображаться.
      </p>

      <VsCode>{`export default function UserCard({ name, email, avatar }) {
  return (
    <div className="user-card">
      <img src={avatar} alt={name} />
      <h3>{name}</h3>
      <p>{email}</p>
    </div>
  );
}

// Можно использовать многократно в любой части приложения
<UserCard name="Анна" email="anna@example.com" avatar="anna.jpg" />
<UserCard name="Петр" email="petr@example.com" avatar="petr.jpg" />`}</VsCode>

      <p>
        Такой подход решает проблему дублирования кода и делает
        интерфейс предсказуемым — один компонент всегда ведет себя
        одинаково при одинаковых входных данных.
      </p>
      <h3>Экосистема и инструменты</h3>
      <p>
        React имеет развитую экосистему библиотек для решения типовых
        задач: React Router для маршрутизации, Redux для управления
        состоянием, Material-UI для готовых компонентов. Это позволяет
        не изобретать велосипед для каждой задачи.
      </p>

      <p>
        Инструменты разработки включают React DevTools для отладки
        компонентов, Create React App для быстрого старта проекта, и
        горячую перезагрузку для мгновенного отображения изменений в
        коде.
      </p>

      <h3>JSX — расширение JavaScript</h3>
      <p>
        React использует JSX — синтаксическое расширение JavaScript,
        которое позволяет писать HTML-подобную разметку прямо в коде.
        JSX компилируется в обычные вызовы функций React.
      </p>

      <VsCode>{`// JSX
const element = <h1>Привет, мир!</h1>;

// Компилируется в:
const element = React.createElement('h1', null, 'Привет, мир!');`}</VsCode>

      <p>
        JSX делает код более читаемым и позволяет использовать всю мощь
        JavaScript для создания динамической разметки с условиями,
        циклами и выражениями.
      </p>

      <h2>Итого</h2>
      <ul>
        <li>
          React — библиотека для создания пользовательских
          интерфейсов, которая решает проблемы масштабирования и
          поддержки сложных приложений
        </li>
        <li>
          Декларативный подход позволяет описывать желаемый результат
          вместо пошагового управления DOM
        </li>
        <li>
          Компонентная архитектура делает код переиспользуемым и
          предсказуемым
        </li>
        <li>
          Развитая экосистема предоставляет готовые решения для
          типовых задач
        </li>
        <li>
          JSX объединяет удобство HTML-разметки с мощью JavaScript
        </li>
      </ul>
      <NavButtons
        prev={"/react"}
        next={"/react/VirtualDom"}
      />
    </section>
  );
}
