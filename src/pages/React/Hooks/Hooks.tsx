import VsCode from "../../../components/VsCode/VsCode.tsx";
import NavButtons from "../../../components/NavButtons/NavButtons.tsx";
export default function Hooks() {

  return (
    <section className={'topic container'}>
      <h2>Что такое хуки в React</h2>

      <h3>Хуки — это способ «оживить» функциональные компоненты</h3>
      <p>Хуки — это функции, которые позволяют использовать возможности React, такие как состояние, жизненный цикл и контекст, внутри обычных JavaScript-функций, без классов.</p>

      <p>До появления хуков (в версии React 16.8) для хранения состояния приходилось использовать классы. Теперь это можно делать проще — с помощью обычных функций.</p>

      <VsCode>
        {`import { useState } from 'react'

function Hello() {
  const [name, setName] = useState('Гость')

  return <h1>Привет, {name}!</h1>
}`}
      </VsCode>

      <p>В примере выше мы используем хук <code>useState</code>, чтобы компонент хранил имя пользователя. Без классов, без constructor, без лишней магии.</p>

      <h3>Зачем вообще нужны хуки?</h3>
      <ul>
        <li>Чтобы управлять состоянием и реакциями на изменения прямо в функциональных компонентах.</li>
        <li>Чтобы делить логику между компонентами без наследования или HOC.</li>
        <li>Чтобы писать меньше кода и лучше его структурировать.</li>
      </ul>

      <h3>Самые популярные хуки</h3>
      <p>React предоставляет несколько встроенных хуков. Вот самые важные из них:</p>
      <ul>
        <li><code>useState</code> — локальное состояние компонента.</li>
        <li><code>useEffect</code> — работа с побочными эффектами (например, запросы).</li>
        <li><code>useRef</code> — сохранение значений между рендерами и доступ к DOM.</li>
        <li><code>useContext</code> — доступ к глобальному контексту.</li>
        <li><code>useReducer</code> — управление сложным состоянием, как в Redux.</li>
      </ul>

      <h3>Почему это удобно?</h3>
      <p>Хуки позволяют вам легко переиспользовать логику между компонентами. Вы можете вынести часть поведения в свой кастомный хук — и использовать его где угодно. Это делает код чище и понятнее.</p>

      <h2>Итого</h2>
      <ul>
        <li>Хуки — это функции, которые добавляют функциональным компонентам возможности классов (состояние, эффекты и т.д.).</li>
        <li>Позволяют писать меньше кода и лучше разделять логику.</li>
        <li>Главные хуки: <code>useState</code>, <code>useEffect</code>, <code>useRef</code>, <code>useContext</code>.</li>
        <li>Работают только внутри компонентов и начинаются с «use».</li>
      </ul>
      <NavButtons prev={'/react/EventHandling'} next={'/react/UseState'}/>
    </section>
  )
}