import howitwork from "../../../images/ReactImages/VirtalDom/howitwork.png"
import NavButtons from "../../../components/NavButtons/NavButtons.tsx";
export default function VirtualDom() {

  return (
    <section className={'topic container'}>
      <h3>Virtual DOM и производительность</h3>
      <p>Однако на этом плюсы React не заканчиваются. Стоит понимать, что все операции над DOM деревом довольно
        ресурсоёмкие и тяжелые.
        Поэтому в React прямое взаимодействие с DOM (добавление, удаление элементов, обработчики событий) сведено к минимуму —
        React сам обновляет интерфейс в ответ на изменения данных.
      </p>
      <p>
        React под капотом использует Virtual DOM — JavaScript-представление
        реального DOM-дерева, но более легковесное и оптимизированное.
      </p>
      <p>
        Это решает проблему производительности — вместо перерисовки
        всего интерфейса обновляются только те элементы, которые
        действительно изменились. Разработчик при этом может писать код
        так, будто весь интерфейс перерисовывается целиком.
      </p>
      <p>
        💡 <strong>Важно понимать:</strong> термин <em>Virtual DOM</em> — это не совсем "виртуальная копия" реального DOM. На самом деле это <strong>абстрактное представление пользовательского интерфейса</strong>, которое React хранит в памяти.
      </p>

      <p>
        Virtual DOM — это структура из обычных JavaScript-объектов, которая описывает, как должен выглядеть интерфейс в данный момент. React сравнивает новое состояние Virtual DOM с предыдущим (процесс называется <em>reconciliation</em>) и обновляет только изменённые части реального DOM.
      </p>
      <p>
        Хотя термин <strong>Virtual DOM</strong> широко используется, правильнее будет называть его <strong>деревом React-элементов</strong>.
      </p>
      <p>
        Это дерево создаётся с помощью вызовов <code>React.createElement</code> (или JSX, который превращается в них после компиляции) и представляет собой структуру, описывающую, что именно должно быть отображено в интерфейсе.
      </p>
      <h3>Как работает Virtual DOM?</h3>
      <p>
        Когда вы пишете код на React, вы работаете не напрямую с реальным DOM, а с его «абстрактной копией» — Virtual DOM. Вот что происходит за кулисами:
      </p>
      <img
        className={`invert`}
        src={howitwork}
        alt=""
        width=""
        height=""
        loading="lazy"
      />
      <ol>
        <li>
          <strong>Рендер компонента → создаётся Virtual DOM</strong><br />
          Вы описываете интерфейс с помощью JSX или <code>React.createElement</code>. React превращает это в дерево JavaScript-объектов — <em>React-элементы</em>.
        </li>

        <li>
          <strong>React сохраняет Virtual DOM в памяти</strong><br />
          Это дерево элементов не связано с реальным DOM — оно существует «в уме» React и описывает, как должен выглядеть интерфейс.
        </li>

        <li>
          <strong>Происходит обновление состояния</strong><br />
          Когда меняется <code>state</code> или <code>props</code>, React пересоздаёт новое дерево Virtual DOM.
        </li>

        <li>
          <strong>Сравнение: React запускает diff-алгоритм</strong><br />
          Новый Virtual DOM сравнивается со старым (этот процесс называется <em>reconciliation</em>). React определяет, какие именно части изменились.
        </li>

        <li>
          <strong>Обновление реального DOM</strong><br />
          После сравнения React минимально и эффективно обновляет только те участки реального DOM, которые действительно изменились. Это делает обновление быстрым и отзывчивым.
        </li>
      </ol>

      <p>
        🎯 В результате вы получаете производительный интерфейс без лишнего ручного труда: не нужно самому писать <code>document.querySelector</code> и следить, что и когда обновилось — React всё берёт на себя.
      </p>
      <NavButtons prev={'/react/Intro'} next={'/react/Setup'} />
    </section>
  )
}